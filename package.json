{
  "type": "module",
  "scripts": {
    "build": "node -e \"require('fs').mkdirSync('dist', { recursive: true })\" && postcss style.css -o dist/input.css",
    "watch": "node -e \"require('fs').mkdirSync('dist', { recursive: true })\" && postcss style.css -o dist/input.css --watch"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.1.15",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.6",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^4.1.15"
  }
}

